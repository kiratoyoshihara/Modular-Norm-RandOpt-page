# Modular Norm RandOpt プロジェクトページ実装方針

## 現時点の決定

- **最新版は真っ白（`#ffffff`）の背景・黒文字とする。** Neural ThicketsのScalingセクションを参考にし、緑の背景、濃緑のコード欄、植物の導入図、ロゴマークを廃止。Overviewに色付きブロックの手法アニメーションを新設し、以前の詳細なMethodセクションは非掲載とする。Inklingはレーダーチャートの操作の参考として残す。
- 最初に作る比較図は、7タスク・3手法を示し、Qwenの3サイズを切り替えるSVGレーダーチャート。仕様は第11節にまとめる。
- レーダーは全タスク・モデルサイズで共通の0–100%尺度とし、点線や系列のホバー／タップ、タスク名のキーボードフォーカスで20・40・60・80・100のガイド目盛りを表示する。値・標本SD・差分はツールチップに表示し、固定の数値欄・数値表・尺度切り替え・操作ガイド・結果要約は表示しない。
- 提案手法は青、RandOptはオレンジ、RMSNorm-onlyは紫。凡例の点を線の中央にそろえ、輪郭・凡例のホバーで同色の面を強調する。スクロールで初めて表示した時は点→線→面の順で描画する。
- 論文のFigure画像を直接貼らない。掲載値からWeb用の図を作り、説明用イラストには再生・停止できる動きを付ける。
- **Figure 2は提供された `figure2.md` を基に新しく再描画する。** 論文と同じ2×2の配置、カテゴリ軸、平均±標本SD、色・マーカー・比較注釈を使用する。Prefix Consistencyの結果図を参考に、行が画面に入った時に軸・曲線・帯・比較注釈を徐々に表示する。カーソル追従の縦横の点線は実装しない。
- **反復手法との比較はAcross tasks & scalesの後に配置する。** 「Comparison with iterative baselines」のCountdown／GSM8Kの2パネルでNと精度を示す。MN-RandOptはN=100・K=25、N=3,000・K=25、N=3,000・K=1の3条件に絞り、星印で表示する。ES-at-Scale・RandOpt・ZO-Finetuner・MeZOと合わせて14点を示し、接続線とSDバーは表示しない。SDはツールチップに残す。
- 実装は静的HTML・CSS・JavaScriptを基本とし、比較図のデータをJSONで管理する。有名な研究サイトから採用する要素と技術上の判断は第12節に記載する。
- 最新の改訂では、OverviewにRandOptの紹介と簡潔な手法図を加え、その後に比較グラフを並べる。比較図の見出しはPCで24px、小画面で21pxの左揃えとし、書体、余白、線、動きのタイミングまで揃える。**pushは最終指示まで行わない。**
- 長さを抑えるため、以前の候補数比較UI・実行時間・モデル系列の表・支持分布分析・投票デモ・末尾の論文案内・フッターを削除する。順序は「タイトル → Overview → Figure 2 → Across tasks & scales → 反復手法との比較」とする。冒頭の著者・所属・リンクボタンを維持し、ページ上部のOverview・Across tasks & scales・Paperのナビゲーションは削除する。以下の調査・旧仕様と異なる場合は、この最新方針を優先する。

## ローカル初期版の実装状況

- 作業ブランチ：`local/research-page`。pushは未実施。
- 起動先：`http://127.0.0.1:8000/`。再起動方法はREADMEに記載。
- タイトルと著者・所属、RandOptの紹介と新しい手法図を含むOverview、Figure 2、反復手法との比較、7軸レーダーを掲載する。以前の詳細なMethodは非掲載。
- レーダーは3サイズ切り替え、共通0–100%目盛り、輪郭・凡例の強調・固定、タスク別のツールチップを備える。静止SVGと凡例を用意し、JavaScriptなしでも1.5Bの比較図を読める。数値表は表示しない。
- 数値63組のTeX照合、DOM上での操作処理、配信とファイル参照を確認した。SVG単体を描画し、PC用・モバイル用のラベルを確認した。
- Chromeでレーダーの表示とホバー／タップ／キーボード操作を確認済み。幅1440px・390px・320pxでラベルの収まりを確認し、JavaScript無効時の静止表示も検証した。
- 旧Methodのソースは復元用に保持するが、表示・初期化・通常の素材生成から外した。新しいOverview手法図と比較図の動きは独立して制御し、スクロール表示と動きを減らす設定に対応する。
- 論文Figureの画像は使わない。Figure 2はseed別の正答数からWeb用SVGを新たに作成した。以前の候補数比較図と導入の植物図は削除した。
- 著者・公開リンク・引用情報は未確定。PDFはGit管理対象外のローカル確認用コピーを使用している。

### 2026-09-21：Overviewの手法アニメーション

- [Iterative RandOptの記事](https://thickets.mit.edu/blogs/iter_randopt.html)の説明とアニメーションを確認。RandOptのGaussian候補生成・Top-K選択・投票を短く紹介し、提案手法が候補生成の幾何を変更することを示す。
- 論文3～4ページのSection 3・Algorithm 1と照合。モジュールの自然ノルム、構造による配分、固定した感度補正を摂動生成に反映する。選別・投票は共通で、反復更新や蒸留は描かない。
- 提供スクリーンショット14.00.06のように長い矢印でつながる一体型の図とする。14.01.51のような濃淡のある正方形タイルで行列を描き、重みは青系、整形済み摂動は暖色系とする。GaussianノイズZのセルを再生ごとにランダムな順序で出現させる。左に未整形のノイズを追加し、「Normalize + scale」の矢印でΔθへつなぐ。各行列は代表的なパラメータテンソルを表し、分割枠を置かずに全体の色の強さを同時に調整する工程を加え、Noise・Scale・Sample・Select・Voteの5段階とする。θとΔθを足し合わせたサンプルは紫系とし、候補と投票モデルも同じ配色を維持する。候補は3×3の9個とし、03・05・07の3モデルを選び、例示回答A/A/BをAに集約する。行列の濃淡・候補番号・回答は模式例として明記する。
- 最初の候補だけNoise → Scaleを詳しく再生し、以降はSample工程に留まって9個を順に送信する。送信開始に合わせてZᵢ・Δθᵢ・θ′ᵢの添字とZ・Δθの各セルの色を更新する。各候補の模様を暖色のノイズ・摂動と紫色のモデルで対応させ、元のθの配色は固定する。生成・正規化の再生は最初の1回のみとする。Noiseは生成1.2秒＋待機1.1秒、Scaleは移動約0.63秒＋保持0.45秒＋濃淡調整2.2秒＋余韻0.4秒。Sampleは約0.83秒間隔で1個ずつ送信し、移動約0.63秒＋到着アニメーション0.38秒で対応する候補を表示する。送信済み候補の出現アニメーションは次の送信が始まっても継続し、完成した候補は残す。9個目の出現完了後に0.95秒（従来の0.45秒に0.5秒追加）待って選別し、3モデルの投票は従来の速度で再生する。全体は約21.5秒。各工程の図・矢印・見出しは開始時に表示し、静止版と動きを減らす設定では完成図を表示する。Pause／Play・Replay・工程ボタン、画面外・非表示タブでの一時停止に対応する。PCは横配置、小画面は縦配置。
- RandOptの紹介リンクは `https://thickets.mit.edu/` に変更し、同じタブで開く。
- ソースは `method-svg.js` / `method-animation.js` / `method-animation.css`、生成は `scripts/build-method.mjs`。従来の詳細なMethodセクションは復活させず、Overview内に組み込む。

### 2026-09-21：Nと精度の反復手法比較（最新版）

- 提供表から、MN-RandOptの各タスク3条件（N=100・K=25、N=3,000・K=25、N=3,000・K=1）と他手法の計14点を採用する。横軸はNの線形軸。NはRandOpt系列では候補数、反復手法では摂動数であり、計算量の比率とは表現しない。
- MN-RandOpt K=25は青い星、K=1の対照は灰色の白抜き星。MeZOとRandOptの結果も表示し、元論文の未報告表記より今回の数値を優先する。K=25の接続線と全手法のSDバーは削除し、標本SDはツールチップに残す。
- N=100とES-at-ScaleのN=3,000を比較する注記を表示。Countdownは38.40%／35.67%（+2.73 pp）、GSM8Kは73.16%／73.11%（+0.05 pp）で、平均値の差を示す。
- Population scalingの点のアニメーションを共用し、軸→N=3,000の点→N=100の点と注記を約1.8秒で表示する。N=100は1.2秒後に表示を始め、注意を向ける。Replayとホバー／タップ／キーボード操作に対応する。キャプションの「Callouts compare mean accuracy…」の説明文は削除する。
- Accuracy (%)は反時計回りに90度回転させ、各パネルの縦軸の左側中央に沿って配置する。小画面でもラベルが収まる余白を確保する。
- ページと左側目次をAcross tasks & scales → Iterative baselinesの順に並べ替える。

### 2026-09-21：小見出しとレーダー目盛りの調整

- 番号付き小見出し「02 Population scaling」「03 Iterative baselines」「04 Across tasks & scales」を削除し、再生成するテンプレートにも反映する。
- ユーザー指定により、全タスク・モデルサイズを共通0–100%に変更。点線のグリッドや系列のホバーで20・40・60・80・100を表示し、タップ／キーボード操作でも利用できるようにする。
- Countdown・GSM8K・MATH-500・Olympiad Benchの下にMath、USPTO-50Kの下にChemistry、MBPPの下にProgramming、ROCStoriesの下にWritingを表示する。静止SVGにも反映する。
- レーダーにもReplayを追加し、選択中のモデルの点→線→面のアニメーションを再生する。ホバー時の図全体の説明「Performance across seven tasks · …」とShared axis行は削除し、図の読み上げ名とスコア・差分のツールチップは維持する。

### 2026-09-21：構成と見出しの整理

- Methodの説明、図、疑似コード、操作部を一旦すべて削除。ヘッダーと左側目次も更新し、再生成で復活させない。
- 研究タイトル・著者・所属・リンクボタンの下に、独立したOverviewを置く。「A model’s architecture tells us how to explore its weights. Use that structure to build stronger ensembles from fewer candidates.」をここへ移す。
- 「Accuracy at a given budget.」「Performance across seven tasks.」等の図の見出しを小さく左揃えにする。意図的な改行を外し、画面幅に合わせて折り返す。
- 表示名Resultsを「Across tasks & scales」に変更。既存リンクとの互換性のため内部ID `results` は保持する。
- 純白背景と既存のグラフデータ・操作を維持。グラフ全体の動きの設定をMethodから独立させる。

### 2026-09-21：反復手法との比較

- [Inklingの紹介記事](https://thinkingmachines.ai/news/introducing-inkling/)の「Controllable thinking effort」を参考に、MNの測定点を結ぶ線と反復手法の点を比較する独自SVGを追加。
- 出典はTable 3(a)、Table D2、Appendix D.2。MNの本実行予算は `200N + KS`、ESは `600000 + S`、MeZO／ZO-Finetunerは開発評価8000回を含む `608000 + S`。調整・準備費用は含めず、トークン数や実行時間の軸として扱わない。
- MNのK=25はN=100、ES予算に合わせたN=2820／2841、N=3000の3測定点。補間線は測定点を結ぶガイドであり、途中の予算で性能を測定したという意味ではない。各点に標本SDを表示する。
- K=1のMN対照も灰色の白抜き丸で表示する。反復手法が返す単一モデルと、K=25アンサンブルの違いを凡例と注記に明示。
- Countdownは1,500問、GSM8Kは1,319問。Figure 2のCountdown（500問）の精度を流用しない。MeZOは元論文で精度が「—」なので点を生成せず「not reported」と記載する。
- スクロール時の段階表示、再生、凡例の強調、点のホバー・タップ・キーボード操作に対応。カーソルに追従する縦横のガイド線は追加しない。既存のセクションナビゲーションを維持し、新しい比較図の項目を追加する。
- 原データは `assets/data/iterative-baselines.json`、再生成は `scripts/build-iterative.mjs`、検証は `scripts/check-iterative.mjs`。静止SVGの描画と数値・DOMの検証を行い、pushはしない。

### 2026-09-20：Figure 2の再描画と段階表示

- 提供MDと論文PDFの6ページを確認。2行×2列、上段Countdown・下段GSM8K、左K=10・右K=25を維持。右列では縦軸の数値を表示しない。
- `assets/data/figure2.csv` に第6節の全40点・120個の正答数を保存。平均と標本SDは整数から再計算する。Countdown・MN・N=100・K=25のSDは `3.22`。Nは25、50、100、200、300の等間隔カテゴリ軸である。
- 指定色（RandOpt `#C76526`、MN `#306FAD`）、破線・白抜き四角／実線・塗りつぶし丸、α=0.09の帯、比較対象の外側マーカー、3×／≥12×の見出しを再現する。
- [Prefix Consistency](https://naoto-iwase.github.io/prefix-consistency-page/) の対象セクションと実装を調査。軸のフェード、左からの曲線表示、帯と注釈の段階表示という動きだけを参考に、独自実装する。コード・図の転用やカーソル追従線の追加はしない。
- 各行をスクロールで初めて表示した時に約3秒で描画。小さく浮き上がる動きとフェードを組み合わせる。再生ボタン、画面外・非表示タブでの停止、全体の停止操作、動きを減らす設定に対応。JavaScriptなしでは完成したSVGを表示する。
- 白背景を維持し、手法と7タスクの結果の間に配置する。小画面でも2×2を保ち、図の領域だけ横スクロールできる。
- 数値・座標・生成SVG・DOMの動作を検証し、SVGを描画して論文との配置を比較。実ブラウザ接続が利用できないため、実ブラウザ上の動きの確認は未実施。ローカルURLで確認できる状態を維持し、pushはしない。

### 2026-09-20：結果表示の簡略化と純白背景（前版の変更履歴）

- 背景をコード欄も含めて `#ffffff` に統一。「Respect what each module does.」以降のモジュール図を緑・青・黄土色に戻した。
- レーダーの常設スコア欄、軸名の下の数値・差分、説明段落、数値表、操作ガイド、結果要約を削除。独立した読み上げ要約も削除し、キーボード操作ではツールチップを参照する。
- RMSNorm-onlyを紫に変更。3手法の輪郭・凡例のホバーで面を強調し、クリック／タップでは固定できる。凡例の点はSVGの線と同じ中心座標にそろえた。
- 初回のスクロール表示では点を順に出し、線をつないで面を表示する。画面外・非表示タブでは停止。明示的な操作、全体のアニメーション停止、動きを減らす設定では完成形をすぐに表示する。
- 末尾の「Explore the full study」「The geometry matters.」とフッター、著者等の未確定情報を案内する文言を削除。
- JSONと論文の全63組の値は一致。静止SVG、DOM操作、描画開始・停止、モジュール図の数式を検証する。pushは行わない。

### 2026-09-20：白黒への変更とセクション削除（前版の変更履歴）

- [Neural ThicketsのScalingセクション](https://thickets.mit.edu/#scaling)を確認し、白背景・黒文字を基本とする。レーダーの3手法の系列色は残し、UI・手法の模式図・コード欄を白黒／グレーに変更。
- `Fewer candidates. Stronger ensembles.` の候補数比較と、`Measured search speedup`、実行時間の数値・注記、同じセクション内のモデル系列表を削除。
- `Where the gain comes from.` の分析、支持分布の分解図、expert数を変える投票デモ、関連注記をまとめて削除。
- ヘッダー・フッターのロゴとfavicon、冒頭の植物・θ₀の図、キャプション、専用の再生ボタンを削除。研究タイトル自体は残す。
- 削除したセクションへのナビゲーション、実行時の初期化、図の生成処理も整理し、再生成で復活しないようにした。
- 残した図のデータ・操作テストを確認。ローカルで反映し、pushは未実施。

### 2026-09-20：説明図とデザインの改訂（前版の変更履歴）

- 読む順序を「研究の要点 → 手法 → 候補数・実行時間 → 7タスクへの転用 → 支持と投票」に変更。
- InterとJetBrains Monoをローカル配信し、ライセンスを同梱。暖かい紙色、森林色のインク、細い罫線、線画の植物、濃緑のコード領域で統一する。
- 手法図は6候補・Top-3の模式図。生成、評価、選択、投票の4段階と疑似コードの該当行が同期する。RandOpt／Modular Normの比較では候補生成だけを変更し、残る処理と模式的なスコアを共通にする。図中のスコア・位置・回答は実測ではない旨を明記する。
- 自然ノルム・質量配分・感度補正をタブで説明する。Table 3(c)から、Full `37.80 ± 0.87`、Frobenius `34.40 ± 0.72`、No recursive scaling `35.80 ± 1.22`、No sensitivity `36.87 ± 1.62` を表示。平均の差はそれぞれ `+3.40`、`+2.00`、`+0.93 pp`。Countdown、Qwen2.5-1.5B、N=100、K=25、3 seedの条件を近くに記載する。棒グラフは0〜40%の軸、誤差線は標本SDとし、有意差を意味する表現は使わない。
- 感度補正の操作は `ρ ∈ [0.5, 2]`、摂動の自然ノルム量は基準の `1 / ρ` 倍。実測の校正値を再現する操作ではなく、式の関係を説明する模式図として扱う。
- 支持の説明にはK=10の模式的な投票を追加。正解Aを出すexpert数を変え、誤答B・Cへの票の分散と同票時の扱いを可視化する。論文の支持分布そのものを作り直した図と誤認させない。
- デスクトップ用と小画面用のSVGを分け、モジュール名と数式の読める大きさを確保。手法の流れは4列から2列へ、コードと説明は縦積みへ変更する。
- 実装原稿は `scripts/method-section.html` / `scripts/support-section.html`、図の生成は `research-visuals.js`、操作は `motion.js` / `research-experience.js`。出典付きアブレーションJSONを追加した。
- 操作と式・値の検証、SVG単体の描画確認を実施。ブラウザ接続が利用できないため、ページ全体の実画面検証は未実施。ローカルサーバーを維持し、pushは行わない。

## 1. 目的と作業の前提

論文「Modular Norm RandOpt: Population-Efficient Ensembling through Architecture-Aware Perturbations」の研究紹介サイトを作成する。初めて研究に触れる読者が、提案の違い、主要な結果、改善の仕組みを順に理解し、論文やコードへ進めるページを目指す。

- 開発とUI確認はローカルで行い、ユーザーのフィードバックを反映しながら仕上げる。
- **pushは最後にまとめて行う。ユーザーが最終的なpushを指示するまで、リモートへの反映や公開設定の変更は行わない。**
- 作業対象は、このドキュメントを置く `Modular-Norm-RandOpt-page` リポジトリとする。
- GitHub操作には、書き込み権限を確認済みの `hiroaki-hamade` アカウントを使う。
- ローカル初期版のHTML・CSS・JavaScriptは実装済み。GitHub Pagesは未設定。
- 初期案ではサイト本文を英語、本書などの作業用ドキュメントを日本語とする。サイトの使用言語は必要に応じて調整する。

## 2. 入力資料と参考サイト

### 論文資料

- [TeXソース](<../main (1).tex>)：本文、数式、表の数値、実験条件の確認に使用する。
- [論文PDF](../Modular_RandOpt___ICLR_2027.pdf)：27ページ。図の見た目と配置、図表の注記を確認する。

上記はリポジトリ外のローカル資料であり、サイトの公開リンクとしては使用しない。論文が更新された場合は、掲載する数値・図・説明を対応する版にそろえる。

### 参考サイトから取り入れる要素

- [Inkling — Thinking Machines Lab](https://thinkingmachines.ai/inkling/)：レーダーの薄いグリッド、強調系列と比較系列の描き分け、切り替え・ツールチップを参考にする。初期版では全体の書体も寄せたが、レビュー後はNeural Thicketsを優先する。
- [Neural Thickets](https://thickets.mit.edu/)：元研究へのオマージュの中心。重み空間に多様な解が分布する図解と、候補生成から投票までの段階的な説明を、本研究の内容に合わせて新しく作る。具体案は第10節に記載する。
- [WONN Project Page](https://jiawen-dai.github.io/WONN_Project_Page/)：中央配置のタイトル・著者情報、概要図、数式を含む手法説明、実験結果を順に読める構成。
- [Prefix Consistency](https://naoto-iwase.github.io/prefix-consistency-page/)：短いメッセージ、余白、研究の仕組みを段階的に説明するアニメーション。

全体の書体・導入・動く図解はNeural Thickets、レーダーの操作はInkling、説明のテンポはPrefix Consistency、詳しい手法・結果の掲載はWONNを参考にする。Nerfies、3D Gaussian Splatting、Distill、Transformer Circuitsも調査し、採用する要素を第12節に整理した。テンプレートのコードを流用する場合は、その利用条件と必要なクレジットを確認する。

### 元論文

- Yulu Gan and Phillip Isola, **Neural Thickets: Diverse Task Experts Are Dense Around Pretrained Weights**（2026）。[arXiv](https://arxiv.org/abs/2603.12228)／[PDF](https://arxiv.org/pdf/2603.12228)。今回確認した版はv1（2026年3月12日）。
- 本研究が継承するRandOptの提案元として扱い、背景説明と図の近くから参照できるようにする。
- 確認箇所は、元論文のFigure 1〜4、Section 4、Algorithm 1と、元サイトの導入図・アルゴリズム説明・結果表示の構成。PDFと図素材は視覚的に確認した。元サイトの配置・操作の設計はHTML/CSS/JavaScriptでも確認したが、ブラウザでのアニメーション再生は未確認。

## 3. 伝える内容と数値の扱い

### 中心となるメッセージ

**モデルの各モジュールに適した摂動を設計することで、より少ない候補から強いアンサンブルを作る。**

提案の変更箇所は候補生成の幾何である。モジュールごとの自然なノルム、構造に基づく摂動量の配分、感度に基づく補正を用いる。候補の評価、top-K選択、plurality votingはRandOptと共通であることを明確にする。

背景として、Neural Thicketsが示した「事前学習済み重みの近傍に多様なタスクexpertが存在する」という見方を紹介し、本研究の「その近傍を、どのような摂動でサンプリングするか」という問いにつなげる。元研究の発見と本研究の貢献をそれぞれ明示する。

### 冒頭で示す主要結果

Qwen2.5-1.5B-Instructの候補数比較を使う。

- Countdown：RandOptの300候補に対し、提案手法は100候補で上回る平均精度。候補数は3分の1。
- GSM8K：RandOptの300候補に対し、提案手法は25候補で上回る平均精度。少なくとも12倍の候補数削減。
- 上記は `K=10` と `K=25` の比較。候補数 `N` と最終アンサンブルのサイズ `K` を区別する。

### 掲載時に維持する条件

- 候補数削減、探索時間の短縮、処理全体の短縮を別の指標として示す。
- `K=25` の実測では、Countdownの探索速度比は `3.10 ± 0.03`、処理全体は `2.48 ± 0.02`。GSM8Kではそれぞれ `12.15 ± 0.84`、`3.49 ± 0.08`。
- 実行時間は論文の機器・推論設定での測定値。再利用する感度プロファイルの校正（約19秒）と事前のハイパーパラメータ探索は計測範囲外であることを注記する。
- Qwenの7タスク・3サイズでは、21条件中14条件でRandOptより高い平均値、1条件で同値。すべてのタスクで改善したという表現は使わない。
- Countdown、GSM8K、MATH-500では、検証したQwenの全サイズで平均値が改善している。
- Llama・Gemmaの比較と、結果が一様でないOLMoの比較を区別する。
- GSM8Kの支持分布の分析では、精度差 `+2.60` ポイントのうち `+2.21` ポイント（約85%）が支持分布の変化に対応する。これは観測結果の分解であり、因果関係の証明として表現しない。
- 平均値、標本標準偏差、seed数、評価条件を元の図表に合わせる。誤差帯を信頼区間と呼ばない。

## 4. ページ構成

### 4.1 タイトルと研究概要

- 正式な論文タイトル、著者、所属、Paper／Codeリンクを配置する。
- タイトル直下に、中心となるメッセージを短い英語で示す。
- 候補数削減の主要結果を、タスク名と比較条件を添えて目立たせる。
- 著者やリンクが未確定の間は、ローカル確認用の仮表示だと分かる形にする。架空の人物・URL・採択情報は掲載しない。

### 4.2 Overview

- 冒頭に元研究への短いクレジットを置き、Neural Thicketsから本研究へのつながりを示す。元論文とプロジェクトページへリンクする。
- Figure 1をもとに、手法、候補数と精度、他タスクへの展開を一望できる導入を作る。
- 論文のFigure画像は貼らず、手法・実測値をWeb用に描き直し、説明文を短く添える。
- 次の段階で、元論文の重み空間の模式図を想起させる独自の比較図を追加する。既存のFigure 1と説明が重複する場合は、導入を一つに整理する。
- モバイルで図中の文字が読みにくい場合は、拡大表示やパネル単位の表示を検討する。

### 4.3 Method

- RandOptが各候補に与えるグローバルな等方的摂動と、Modular Norm RandOptのモジュール別ノルム・スケールを対比する。
- 「摂動を生成 → 候補を評価 → top-Kを選択 → 投票」の流れを図で示す。
- 元サイトの段階表示を参考に、候補生成の違いを強調し、評価・選択・投票は共通の流れとして描く。具体的な表示と説明上の注意は第10節に従う。
- 数式は提案の理解に必要なものに絞り、記号の意味を近くに置く。
- 詳細な校正手順は短い補足や論文への案内にまとめる。

### 4.4 Results

- 候補数と精度差をSVGで示す。Figure 2の曲線を画像として貼ったり、未提供の曲線データを見た目から推定したりしない。
- 探索時間と処理全体の時間を分けて紹介する。
- タスク・モデルサイズへの展開には、Table 1(a)に対応するInkling風のレーダーチャートを置く。モデルサイズを切り替え、凡例とツールチップから比較を読めるようにする。
- モデル系列への展開はTable 1(b)に対応する別の図または表で示す。評価タスクが異なる結果を、同じ7軸のレーダーに混ぜない。
- レーダーチャートはタスクごとの手法比較の図とし、研究の中心的な候補数削減は候補数・精度差の図と実測時間で示す。

### 4.5 Why it works

- Figure 3〜5の分析内容を、出典付きの数値・Web用の図・説明で紹介し、選択後の正解支持分布へつなげる。
- 正解を支持するexpert数と投票結果の関係を、短い説明図で補助する。
- 実験から得た分析と、説明のための模式図を見分けられるようにする。

### 4.6 Citationとフッター

- 公開情報が確定した段階でBibTeXを掲載し、コピー操作を用意する。
- 本研究の引用情報に加えて、元論文の著者・論文・サイトへのリンクを持つ関連研究の短い項目を設ける。オマージュの図にも出典を添える。
- 必要に応じて利用したテンプレートのクレジットを記載する。
- DOI、arXiv ID、公開年、採択先などの未確認情報は補完しない。

## 5. 見た目と操作

- Neural Thicketsを参考にした控えめな研究ページの見出し、明るい生成り背景、植物の緑と重み空間の図を基本にする。図は枠で囲いすぎず、見出し・操作・図・説明の順に配置する。
- ページの主見出しと操作UIはシステムのサンセリフ体とする。図中の数式など、必要な部分だけセリフ体を使う。
- 提案手法はInklingを参考にしたコバルトブルー `#0155BF`、RandOptはオレンジ、補助比較は落ち着いたグレーを基本とする。色だけでなく線種・点の塗り・ラベルでも識別できるようにする。
- オマージュの図では、元論文を想起させる中心の重み・周辺の解領域・探索範囲という要素を取り入れる。白背景と青／オレンジの比較色は維持し、図中のタスク色とは凡例を分ける。
- ページの主要領域はおよそ960〜1,120px、説明文は620〜700px、レーダーチャートは約860pxを最大幅の目安とする。
- タイトル、著者、リンクは中央配置。本文は見出しと図のまとまりを保つ。
- スマートフォンでは1列にする。レーダーチャートは全体の形が一画面に収まるようにラベルを再配置し、横スクロールを前提にしない。横長の表・数式は必要に応じて局所的な横スクロールを使う。
- アニメーションには再生・一時停止・再実行の操作を用意する。`prefers-reduced-motion` では静止状態でも説明が伝わるようにする。
- ボタンのフォーカス表示、キーボード操作、画像の代替テキスト、見出し階層を整える。

## 6. 技術構成

### 基本方針

- GitHub Pagesで配信できる静的なHTML・CSS・JavaScriptで実装する。
- 初期版ではReactやビルド工程を導入せず、ローカルで簡単に起動・修正できる構成にする。
- 主要な説明と図はHTMLに置き、JavaScriptはアニメーション、表示切り替え、コピー操作などに使う。
- レーダーチャートと手法の模式図にはSVGを使う。レーダーの座標、線、点、ラベルを自前で描き、タブ・凡例・ツールチップはHTMLとJavaScriptで構成する。
- 初期の7軸・3系列には専用の小さな描画処理を使う。Canvas、WebGL、大規模なグラフライブラリの導入は、後から必要性が生じた場合に検討する。
- 実測値は `assets/data/transfer-results.json` を正とし、図・ツールチップ・数値表を同じデータから作る。JavaScriptが使えない場合にも静止版と実験条件を読めるようにする。
- 数式表示が必要な場合はKaTeXなどを使用し、採用時にバージョンと導入方法を確認する。
- 画像やスクリプトには相対パスを使い、GitHub Pagesのリポジトリ名を含むURLでも動作させる。

### 想定するファイル構成

```text
Modular-Norm-RandOpt-page/
├── README.md
├── IMPLEMENTATION_PLAN.md
├── index.html
├── .nojekyll
└── assets/
    ├── css/
    │   └── style.css
    ├── data/
    │   ├── transfer-results.json
    │   └── population-comparison.json
    ├── js/
    │   ├── main.js
    │   ├── radar-chart.js
    │   ├── radar-svg.js
    │   ├── budget-chart.js
    │   ├── illustrations.js
    │   └── motion.js
    └── figures/
        └── transfer-radar.svg
```

`assets/papers/` にはGit管理外のローカル確認用PDFを置き、公開前に正式な資料へ置き換える。`transfer-radar.svg` は同じ数値データから生成する静止版とし、データ更新時に再生成する。`illustrations.js` は独自SVGを生成し、`motion.js` が再生と段階を制御する。生成用スクリプトは `scripts/` に置く。

### 図とデータ

- `figure1.pdf`〜`figure5.pdf` の元ファイルは現時点で未同梱。論文の図画像を抽出して貼る方法は採用しない。TeXの実測値を使い、Web上で描画する。
- 元のベクター図を入手できた場合は、文字と線が鮮明に表示できる形式で置き換える。
- スマートフォンと高解像度画面で文字の読みやすさを確認する。
- 数値を再描画する場合はTeXの表または提供された実験データを使う。グラフの見た目から精密な数値を推測しない。
- 図からしか確認できない系列は、元データ入手まで曲線を再現しない。数値が明記された別の比較で結果を伝える。
- 説明用アニメーションは模式的な表現であることを示す。実測の軌跡や追加実験と誤認させない。

## 7. 実装の順序

1. **初期UIと比較図**：ローカルの作業ブランチを用意し、タイトル周辺、主要結果、著者・リンクの仮表示と、実測値を使ったレーダーチャートを作る。まずQwen 1.5Bの静的表示を整え、その後3サイズの切り替えと数値表示を加える。
2. **UI確認**：ローカルサーバーを起動し、PCとスマートフォンで余白、書体、線、ラベル、操作感を確認する。全体はNeural Thickets、比較図はInklingを参考に、ユーザーのフィードバックを反映する。
3. **説明の充実**：手法、実験結果、分析のセクションを追加し、論文と数値・条件を照合する。
4. **動きと操作**：説明を助ける箇所にアニメーションや表示切り替えを加える。動きのない状態でも内容を読めることを確認する。
5. **公開情報の確定**：著者、所属、Paper／Code、BibTeX、公開用PDFを確定し、タイトル・説明文・共有用画像などのメタ情報を整える。
6. **最終確認と反映**：ローカルで完成状態を確認し、変更内容を整理する。ユーザーから最終pushの指示を受けた後、まとめて反映する。

各段階でローカルの変更を確認できる状態にする。GitHubへのpushをプレビューの前提にしない。

オマージュ要素は、第10節の優先順に沿って、出典付きの導入、静的な比較図、段階的なアニメーションの順で加える。

## 8. ローカルでの確認方法

リポジトリのルートで以下を実行する。

```sh
python3 -m http.server 8000 --bind 127.0.0.1
```

ブラウザで `http://127.0.0.1:8000/` を開く。サーバーは `Ctrl+C` で終了する。

確認する項目：

- 幅390px、768px、1,440pxを目安に、文字・画像・図表の表示と横はみ出しを確認する。
- 見出しから主要結果までの読み順と、冒頭で研究の違いが伝わるかを確認する。
- 画像・CSS・JavaScriptの読み込み、数式、外部リンク、ページ内リンクを確認する。
- レーダーチャートの3サイズ・3手法・7タスクが元の表と一致し、タブ切り替え後も軸・凡例・値が対応していることを確認する。
- 凡例のマウス・キーボード・タッチ操作、ツールチップ、数値表、長いタスク名、欠測値の扱いを確認する。レーダー全体がスマートフォンで欠けないことを確認する。
- アニメーション操作、キーボード操作、動きを減らす設定での表示を確認する。
- 元研究の説明・本研究の貢献・模式図・実測結果が区別でき、元論文へのリンクが適切な位置にあることを確認する。
- 公開前には、予定するGitHub Pagesのサブパスを模したURLでも素材の参照を確認する。
- 自動操作できるブラウザが利用できない場合は、その制約を明示する。ユーザーの手元のブラウザで表示を確認し、未実施の表示確認を完了扱いにしない。

静的ページの初期実装では、画面・リンク・掲載内容の確認を優先する。複雑なデータ処理を追加した場合に、その処理に必要なテストを追加する。

## 9. 未確定事項と公開準備

以下は初期レイアウトの作成を止めずに、実装と並行して確定する。

- 著者名、著者順、所属、各著者のプロフィールURL。
- 論文・コードの公開URL、公開用PDF、掲載する公開状況とBibTeX。
- 図の元ファイルと、候補数・精度の曲線などを再描画する場合の数値データ。レーダーチャートに使うTable 1(a)の数値は、提供済みのTeXから取得できる。
- 最終的な説明量と、アニメーションを入れる箇所。

GitHub Pagesの標準URLを使う場合の公開先候補は、リポジトリ所有者に基づく `https://kiratoyoshihara.github.io/Modular-Norm-RandOpt-page/` となる。作業用GitHubアカウント名とは区別する。

確認時点では `hiroaki-hamade` にpush権限はあるが、管理者・maintainer権限はない。Pagesの初期設定については、公開準備の段階で所有者側の対応または必要な権限を確認する。現段階では設定を変更しない。

公開準備完了の条件は、研究内容と数値が論文に対応していること、著者・公開リンクなどの仮表示が解消していること、PCとスマートフォンでUIが確認できていること、ユーザーがローカルの完成状態を確認できていることとする。

## 10. Neural Thicketsへのオマージュ

### 10.1 取り入れる考え方

元サイトは、解が見つかりにくい状況と、解が豊富にある状況を、荒れ地と茂みのイラストで対比している。さらに、事前学習済み重みを示す星、タスクごとの色付き領域、探索範囲を示す破線を使い、重み空間の説明へ移る。RandOptの処理は図と疑似コードで段階表示し、結果はタスク・モデル別に切り替える構成になっている。[参考：元サイト](https://thickets.mit.edu/)

本ページでは、**「解が茂る近傍を、モデル構造に合った摂動で探索する」**という流れをオマージュの軸にする。初期案では重み空間の模式図を優先する。森や枝葉の装飾を加える場合は、導入図の補助的な表現として用いる。

### 10.2 具体的に追加する要素

#### A. 元研究へのクレジットと導入文

Overviewの冒頭に、著者名とリンクを含む1〜2文の説明を置く。英文の初期案は次のとおり。この文は本サイト用の説明案であり、元論文からの引用ではない。

> Building on RandOpt from Neural Thickets (Gan & Isola, 2026), we study architecture-aware perturbations for population-efficient ensembling.

必要であれば、短い補助見出しとして `Exploring neural thickets with architecture-aware perturbations` を使う。正式な論文タイトルは維持する。

#### B. 同じ重み近傍でサンプリングを比較する図

- 中央の星を事前学習済み重み、周辺の色付き領域をタスクに適した解の模式表現とする。元論文Figure 1へのリンク付き注記を添え、SVGで新しく描く。
- RandOptとModular Norm RandOptを左右に置き、同じモデル・同じ中心重み・同じ背景の解領域に対して、摂動の作り方を比較する。
- RandOpt側は等方的なノイズ、提案側はEmbedding・Attention・MLP・Normなどのモジュールごとの正規化とスケールを示す。提案側にはモジュールの小さな模式図を添える。
- 候補が現れ、評価され、選択された候補が投票へ進む流れを、まず静止図として作る。その後、必要な箇所をアニメーション化する。
- 両手法の背景の解領域は共通に保つ。提案手法が解そのものを増やした、モデル規模を大きくした、またはあらゆるタスクの解密度を高めたと受け取られる描写を避ける。
- 模式図であることを注記する。候補点の描画数は説明用とし、実測の300対25候補の比較は、対応するグラフと条件付きの数値で示す。

#### C. 継承した処理と変更箇所の段階表示

- 元サイトと元論文Algorithm 1の説明形式を参考に、候補生成・評価・top-K選択・投票を一つの流れにする。
- 候補生成の段階では、RandOptと提案手法を切り替えるか並べて表示する。モジュール別の正規化・スケールの部分を強調する。
- 評価・選択・投票には共通のラベルと配置を使い、継承している処理が分かるようにする。
- 初期実装は短いステップ説明と図で構成する。疑似コードの同期表示は、情報量を確認してから追加する。
- 再生・一時停止・再実行、動きを減らす設定、モバイルでの縦配置を用意する。

#### D. 結果の切り替えと分析への接続

- 第11節のレーダーチャートには、初期版からモデルサイズの切り替えを用意する。それ以外の結果で情報量が増えた場合は、元サイトを参考にタスク・モデルの切り替えを追加する。
- 本研究の主図では候補数 `N` と精度の関係を示す。元論文のモデル規模に関する結果と、比較軸を区別する。
- Why it worksでは、元研究の解密度・多様性の見方から、本研究の選択後の正解支持分布の分析につなげる。

### 10.3 科学的な説明と帰属

- 元論文Section 4のRandOptは、複数のグローバルなノイズ強度から候補ごとに値を割り当てる設定を含む。「元RandOptは常に一つの固定σだけを使う」という説明にしない。比較の中心は、各候補内の等方的摂動と、モジュール別のノルム・スケールの違いである。[根拠：元論文Section 4・Algorithm 1](https://arxiv.org/pdf/2603.12228)
- 提案手法はガウスノイズをモジュールごとの自然なノルムで正規化している。単に円を楕円に変えたガウス分布として定義せず、図の近くで正規化とスケールの意味を説明する。
- 本研究の測定条件で再評価したRandOptと、元論文掲載の結果を区別する。元論文の曲線や数値を、本研究の比較結果として混在させない。
- 元研究の名称・著者・リンクは、背景説明と図の出典として表示する。サイトの著者・所属欄には本研究の確定情報を使い、MITのロゴや元著者の所属表示をサイトのブランドとして転用しない。
- オマージュ用の図・文章・実装は新しく作る。元の図やコードを直接利用する場合は、利用条件を確認し、対象と出典を明記する。

### 10.4 実装の優先順

1. **初期版に含める**：元論文・サイトへのリンク、研究のつながりを示す導入文、静的な処理の比較図。
2. **最初のUI確認後に加える**：重み近傍の図解を使った段階的なアニメーションと、候補生成の切り替え。
3. **必要に応じて加える**：疑似コードとの同期、レーダーチャート以外の図のタスク・モデル切り替え、茂みを想起させる装飾。

オマージュの配置と強さもローカルで確認しながら調整する。全体はNeural Thicketsへ寄せ、比較図の操作には第11節のInkling調査を生かす。

## 11. Inklingを参考にした比較図とレビュー後の調整

### 11.1 確認した図の構成

ユーザー指定の対象は、[Inklingの「Well-rounded, generalist models」](https://thinkingmachines.ai/inkling/)にあるレーダーチャートである。公開HTML、CSS、SVGの構造、描画と操作のJavaScriptを確認した。今回の環境ではブラウザを接続できていないため、実画面のスクリーンショットと操作感の検証は未実施。以下はソースから確認した構成である。

- タブで表示モデル群を切り替え、チャート下に操作できる凡例を置いている。
- 10軸・0〜100の値をSVGに描画する。主系列はコバルト色の実線と塗りつぶした点、比較系列は異なる色・破線・中抜きの点で識別する。
- 主系列の面を薄く塗り、グリッドと比較系列を控えめにしている。
- 凡例や系列へのポインター操作、凡例へのフォーカス・クリックで対象を強調する。指標名へのポインター操作で各系列の値を表示する。
- スマートフォン用のスタイルでは図全体を縮尺に合わせ、図中の文字を相対的に大きくする。
- SVGはJavaScriptで生成し、画面に近づいた時点で初期化する。登場時の動きには、動きを減らす設定への対応がある。

### 11.2 本研究用に作る一つの図

図の仮題は **Performance across seven tasks** とする。Inklingの見た目・構成を参考に、本研究のタスク別評価を示す。

- **配置**：Results内のタスク・モデルサイズへの展開を説明する場所。候補数削減の主張を示すグラフとは分ける。
- **データ出典**：本研究のTable 1(a)、TeXの `tab:transfer_runtime`、PDF 5ページ。
- **条件**：`N=100`、`K=25`、seeds 42〜44の平均と標本標準偏差。
- **タブ**：Qwen2.5-Instructの `0.5B / 1.5B / 3B`。初期表示は主な候補数・時間比較でも使う `1.5B`。
- **7軸**：Countdown、GSM8K、MBPP、ROCStories、USPTO-50K、MATH-500、OlympiadBench。論文の表と同じ順序を固定し、上端から時計回りに配置する。
- **3系列**：Modular Norm RandOpt、RandOpt、RMSNorm-only。3系列すべてを初期表示する。
- **単位**：各タスクのスコア（%）。USPTO-50Kはbalanced accuracy、他は論文に定義されたaccuracyであることを図の直下に明記する。

各軸は、そのタスクのselection setで選ばれたアンサンブルの評価である。単一の固定アンサンブルが7タスクを解いたという意味にはしない。転用される摂動プロファイルと、タスクごとに行う候補選択をキャプションで説明する。

### 11.3 軸と実測値のルール

- 初期の共通0〜100表示では差が小さいという指摘を受け、タスク別の範囲拡大を初期表示にする。3手法の平均±標本SDを含む範囲に余白を取り、読みやすい端点へ丸める。各軸に中心から外周までの範囲を明記する。
- 共通0〜100表示にも切り替えられるようにする。拡大時はモデルサイズに応じて範囲が変わることを注記し、異なる尺度での面積を総合スコアとして読ませない。
- 拡大時は各範囲を5等分するグリッド、共通尺度では20刻みのグリッドを置く。線と点は平均値を表し、標本SDはツールチップと数値表に示す。各タスクの近くにRandOptとの差も表示する。
- ROCStoriesなどの低い値や、手法間の小さな差は形だけでは読みにくい。図の下に「数値を見る」を置き、平均・標本標準偏差・RandOptとの差を確認できるようにする。
- 差はpercentage points（pp）として表示する。例えばQwen 1.5BのGSM8Kは、提案手法 `74.20 ± 0.32`、RandOpt `68.39 ± 1.12`、平均値の差は `+5.81 pp`。
- 7軸の面積を総合スコアとして扱わず、元の個別タスクの評価が読み取れるようにする。改善しないタスクも含めて表示する。
- 欠測値は `null` として保持し、表示は「N/A」とする。実測ゼロと区別し、欠測をまたいだ面や線を実測結果のようにつながない。
- 数値は3サイズ×3手法×7タスクの63組の平均・標本標準偏差を転記する。タスク名、指標名、条件、出典もデータに持たせる。

### 11.4 図の見た目

- 白地に、見出し、短い条件説明、サイズ切り替え、図、凡例、注記の順で配置する。
- 提案手法は `#0155BF`、実線、塗りつぶした点。比較系列はオレンジとグレー、異なる破線、中抜きの点にする。
- 提案手法の線は約2.8〜3px、比較系列は約2pxを初期値とする。主系列の面は約10〜12%の不透明度とし、背景や他系列を隠さない。
- ラベルは図の周囲に配置し、長いタスク名は改行する。文字の読みやすさを維持したうえで、グリッドと軸線を薄くする。
- 凡例は小さなボタンとして並べ、選択状態には控えめな背景を付ける。本文や図全体に大きな影・装飾枠を増やさない。
- 本研究の図、数値、文章で作り、Inklingのモデル名やデータは掲載しない。書体は利用できるフォントで再構成する。

### 11.5 操作と読みやすさ

- サイズタブを切り替えると、同じ軸順でデータ、条件表示、数値表を更新する。拡大尺度では範囲も更新し、共通尺度では0〜100を保つ。
- 凡例のhover／focusで対象系列を一時的に強調し、click／tapで強調を固定する。もう一度選ぶかEscapeで固定を解除する。比較系列は薄く残す。
- 軸ラベルや点から、そのタスクの3手法の値を確認できるようにする。ポインター操作に加え、フォーカスとタップでも到達できるようにする。
- タッチ画面では読取欄を図の下に置くなど、指で値が隠れず、画面外にはみ出さない表示にする。
- スマートフォンでは7軸の全体形状を保ち、ラベルの折り返しと余白を調整する。縮小で文字が小さくなりすぎる場合は、モバイル用の配置を設ける。
- 数値表にはタスク名・手法名・単位を付ける。SVGにもタイトルと説明を用意する。
- 登場と切り替えには短いトランジションを使い、常時動き続ける演出は加えない。`prefers-reduced-motion` では動きを省略して最終状態を表示する。

### 11.6 実装方法と確認項目

座標計算は7軸の角度と平均値から行い、SVGの線・面・点・文字を更新する。操作状態は、選択中のモデルサイズ、強調中の手法、表示中のタスクに分けて管理する。図、数値表、静止版には同じJSONを使う。スクロール時は初回表示の判定だけを行い、スクロールのたびに図を作り直さない。

実装時には次を確認する。

- 63組の平均・標本標準偏差がTeXと一致すること。
- 拡大尺度の下限・上限が中心・外周に対応し、平均±標本SDが範囲内に入ること。共通尺度は中心0・外周100で固定され、どちらも軸順が変わらないこと。
- 強調やツールチップが古いタブの値を残さず、ラベルと系列が常に対応すること。
- 390px幅でも図が欠けず、長いラベル・凡例・注記を読めること。
- キーボード、タッチ、動きを減らす設定、JavaScript無効時の静止版で内容を確認できること。

SVGの座標系は[MDNのSVG要素の説明](https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Element/svg)、タブの意味と操作は[WAI-ARIA Tabs Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/)、動きの制御は[MDNのprefers-reduced-motion](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion)を参照する。

## 12. 研究サイトの追加調査と採用方針

以下は各サイトの公開内容とHTMLを確認して整理したもの。各サイトから何を取り入れるかは本研究に合わせた設計上の判断であり、元サイトがこの構成を推奨しているという意味ではない。いずれも実ブラウザでの操作検証は未実施。

### 12.1 Nerfies

[Nerfies: Deformable Neural Radiance Fields](https://nerfies.github.io/)は、論文情報とリンク、冒頭の成果映像、説明、具体例、補間スライダー、関連研究、BibTeXを順に配置している。ソースでは静的HTMLとBulma・JavaScriptを使っている。

本研究では、著者・Paper／Codeへの分かりやすい導線と、説明した直後に具体例を見せる順序を採用する。見た目は本ページの方針に統一し、テンプレート一式の導入を前提にしない。

### 12.2 3D Gaussian Splatting

[3D Gaussian Splatting](https://repo-sam.inria.fr/fungraph/3d-gaussian-splatting/)は、成果映像、品質・速度の評価、手法間の画像比較を分けて示している。比較部分では、同じ対象の画像を重ねて確認する構成を使っている。

本研究では、精度と時間の指標を区別すること、同じ条件の比較を隣に置くことを採用する。RandOptと提案手法の模式図も同じ中心重み・共通の処理でそろえ、変更箇所を読み取れるようにする。

### 12.3 Distill — Feature Visualization

[Feature Visualization](https://distill.pub/2017/feature-visualization/)は、本文の問いに沿って図、短い説明、数式、例を近くに配置し、段階的に内容を掘り下げる構成を使っている。図にはSVGと画像を使い分けている。

本研究では、手法の図に記号の説明を添えること、Why it worksを一段ずつ説明すること、詳しい条件を図の近くで読めるようにすることを採用する。数式や長い実験条件は本文の流れを保つ位置に置く。

### 12.4 Transformer Circuits

[A Mathematical Framework for Transformer Circuits](https://transformer-circuits.pub/2021/framework/index.html)は、概要・目次・数式と図を持つ長い解説に、対象をhoverで強調しclickで固定する探索UIを組み合わせている。ソースでもattention headの強調・固定操作を確認した。

本研究では、レーダーの系列や手法図のモジュールを一時強調し、必要なら固定して読む操作を採用する。最初のページは短い説明を中心にし、詳しい数式・分析には段階的に進めるようにする。

### 12.5 調査から決めた実装の範囲

- **最優先**：Neural Thicketsに寄せた動く図解と、差を読みやすくした実測値の7軸レーダーチャート。
- **次に作るもの**：Neural Thicketsへのクレジットと、モジュール別の摂動を説明するSVGの模式図。
- **比較の原則**：モデル、タスク、候補数、アンサンブルサイズ、指標を近くに記し、実測結果の比較条件を保つ。
- **技術**：HTML・CSS・JavaScript・SVG・JSONで完結させる。参考サイトのフレームワークを、そのまま導入する必要はない。
- **情報設計**：短い主張、図、近接する説明、必要な詳細という順序を基本にする。レーダー以外の図も線・色・書体をそろえる。
- **次の作業単位**：タイトル周辺とレーダーチャートを実装し、ローカルでUIを確認する。そのフィードバックを受けて他セクションへ展開する。

調査方針に沿ったローカル初期版を実装した。現在の範囲と確認状況は冒頭の「ローカル初期版の実装状況」を参照する。pushと公開設定の変更は未実施。
